import css from './CartPopup.module.scss';

export default function CartPopup({ cartProducts, productData }) {

    console.log(cartProducts, productData);
    
    return (
        <div>
        </div>
    )
}